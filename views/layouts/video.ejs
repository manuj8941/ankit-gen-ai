<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= page.title %> - Gen AI Docs</title>
        <link rel="stylesheet" href="/css/main.css">
    </head>

    <body>
        <%- include('../partials/header', { rootPages, currentSlug }) %>

        <div class="container">
            <%- include('../partials/breadcrumbs', { ancestors, page }) %>

            <div class="content-wrapper">
                <%- include('../partials/sidebar', { rootPage, fullSidebar, currentSlug }) %>

                <main class="main-content video">
                    <article>
                        <h1><%= page.title %></h1>
                        <div class="video-container">
                            <!-- Video will be embedded here based on content -->
                            <%- page.content %>
                        </div>
                    </article>
                </main>
            </div>
        </div>

        <%- include('../partials/footer') %>
    </body>

</html>